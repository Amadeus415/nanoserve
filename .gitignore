# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
build/
dist/

# environments / tooling
.DS_Store
.env

# ad-hoc bench.py artifacts at the top level are throwaway
results/*.jsonl
results/*.png

# NOTE: lab.py writes to results/<experiment-id>/ and those ARE tracked on
# purpose — runs.jsonl, env.json, report.md and charts are the lab notebook.

# model weights are never committed
*.safetensors
*.gguf
weights/
